#include <stdio.h>

int main() {
    
    long long int A;
    long long int B;

    long long int Multiply;

    scanf ("%lld %lld", &A , &B);
    Multiply = A * B;
    
    printf ("%lld\n", Multiply);

 
    return 0;
}